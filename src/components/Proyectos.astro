---
import { getCollection } from "astro:content";

const proyectos = await getCollection('projects')

---
<div aria-hidden="true" class="h-8 md:h-16 lg:h-24"></div>
<section id="proyectos" class="scroll-mt-32 pt-36 pb-16 translate-y-10">
    <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-purple-900 mb-8 text-center">Proyectos</h2>
    <p class="text-lg md:text-xl text-gray-700 mb-16 text-center max-w-3xl mx-auto"> Esta es la seccion donde mostrare mis habilidades y los proyectos que he realizado a lo largo de mi carrera y universidad</p>
    <div class="grid gap-6 grid-cols-1 md:grid-cols-2 lg:grid-cols-3 justify-center px-14">
        {proyectos.map((proyecto) => (
            <article class="group relative bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-2xl transition-all duration-500 hover:-translate-y-2">
                    {proyecto.data.img && (
                        <div class="relative h-48 overflow-hidden bg-gradient-to-br from-purple-400 to-purple-600">
                            <img 
                                src={proyecto.data.img} 
                                alt={proyecto.data.title}
                                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                            />
                            <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                        </div>
                    )}
                    
                    <!-- Contenido -->
                    <div class="p-6 space-y-4">
                        <!-- Header con título y fecha -->
                        <div class="flex items-start justify-between gap-2">
                            <h3 class="text-xl font-bold text-purple-900 group-hover:text-purple-600 transition-colors duration-300 line-clamp-2">
                                {proyecto.data.title}
                            </h3>
                            {proyecto.data.time && (
                                <span class="text-xs text-gray-500 whitespace-nowrap bg-purple-100 px-2 py-1 rounded-full">
                                    {proyecto.data.time}
                                </span>
                            )}
                        </div>
                        
                        <!-- Descripción -->
                        <p class="text-gray-600 text-sm leading-relaxed line-clamp-3">
                            {proyecto.data.description}
                        </p>
                        
                        <!-- Footer con autor y botón -->
                        <div class="flex items-center justify-between pt-4 border-t border-gray-100">
                            {proyecto.data.author && (
                                <div class="flex items-center gap-2">
                                    <div class="w-8 h-8 rounded-full bg-gradient-to-br from-purple-400 to-purple-600 flex items-center justify-center text-white text-sm font-semibold">
                                        {proyecto.data.author.charAt(0).toUpperCase()}
                                    </div>
                                    <span class="text-sm text-gray-600">{proyecto.data.author}</span>
                                    {proyecto.data.coAuthor && (
                                        <div class="w-8 h-8 rounded-full bg-gradient-to-br from-purple-400 to-purple-600 flex items-center justify-center text-white text-sm font-semibold">   
                                            {proyecto.data.coAuthor.charAt(0).toUpperCase()}
                                        </div>
                                        <span class="text-sm text-gray-600">{proyecto.data.coAuthor}</span>
                                    )}
                                </div>
                            )}

                        </div>
                    </div>                    
                </article>
        ))}
    </div>
</section>